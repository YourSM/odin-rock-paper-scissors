function getComputerChoice() {
  let x = Math.random();
  if (x >= 0 && x <= 0.3) {
    y = "Rock";
  } else if (x >= 0.4 && x <= 0.6) {
    y = "Scissors";
    } else y = "Paper";
    alert(y)
  }

getComputerChoice()


// create function getComputerChoice
// Get getComputerChoice to return one of three options
// Math.Random() * 10.
// Use math.Random to create numbers 0-1
  // split results three ways and assign them to each option.


// function getComputerChoice()
  // x = Math.Random()*10
  // if x >= 0 && x <= 0.3 {
    // y = "Rock"
  // } else if x >= 0.50 && <= 0.6 {
    // y = "Scissors"
    // } else y = "Paper"
    // console.log(y) 